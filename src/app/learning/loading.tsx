import RouteStateCard from '@/components/route-fallbacks/RouteStateCard';

export default function Loading() {
  return <RouteStateCard eyebrow="Loading" title="Loading learning center" message="Gathering courses, CBTs, and training resources." />;
}
