import RouteStateCard from '@/components/route-fallbacks/RouteStateCard';

export default function Loading() {
  return <RouteStateCard eyebrow="Loading" title="Loading pilot resources" message="Rendering the pilot briefing maps and reference sections." />;
}
